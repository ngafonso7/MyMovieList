

In order to build this application, you just have the API_KEY from TheMovieDb service.
To use the API_KEY, you must add a gradle.properties file with the property:
- the_movie_db_api_key="XXX" , where XXX is the API_KEY as String.